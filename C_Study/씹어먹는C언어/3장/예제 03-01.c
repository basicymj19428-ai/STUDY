#include <stdio.h>

int main(void)
{
	int a = 10;
	int b = 3;

	printf("a + b는 %d\n", a + b);
	printf("a - b는 %d\n", a - b);
	printf("a * b는 %d\n", a * b);
	printf("a / b는 %d\n", a / b);
	printf("a %% b는 %d\n", a % b);  //%% : %를 표시하기 위한 방법

	return 0;
}